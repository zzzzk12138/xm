<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaomi.warn.mapper.warnMapper">
    <!-- 保存预警信息 -->
    <insert id="saveWarn">
        INSERT INTO warn (car_id, bat_typename, warnName, warnLevel)
        VALUES (#{carId}, #{batTypename}, #{warnName}, #{warnLevel})
    </insert>

    <!-- 查询指定车辆的预警信息 -->
    <select id="findWarnsByCarId" resultType="com.xiaomi.warn.entity.Warn">
        SELECT 
            *
        FROM warn
        WHERE car_id = #{carId}
          AND is_deleted = false
        ORDER BY created_at DESC
    </select>

</mapper> 